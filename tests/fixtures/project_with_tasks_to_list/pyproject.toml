[tool.poetry]
name = "taskipy"
description = "tasks runner for python projects"
version = "1.0.0"
authors = ["foobar"]

[tool.taskipy.tasks]
one = "echo first task"
two = "echo second task"
three = "echo third task"
